{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/openai-img-proxy/:path*",
      "destination": "https://images.openai.com/:path*"
    }
  ]
}
